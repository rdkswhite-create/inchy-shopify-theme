{% comment %}Use for visible brand copy only, never URLs, metadata or botanical names.{% endcomment %}
{%- if rich_text -%}
  {{- text | replace: 'Inchy', 'INCHY' | replace: 'glp-1', 'GLP-1' | replace: 'Glp-1', 'GLP-1' -}}
{%- else -%}
  {{- text | escape | replace: 'Inchy', 'INCHY' | replace: 'inchy', 'INCHY' | replace: 'glp-1', 'GLP-1' | replace: 'Glp-1', 'GLP-1' -}}
{%- endif -%}
