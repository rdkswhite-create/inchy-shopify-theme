{% doc %}
  Renders the U.S. launch form on product and guide pages.
  @param {string} instance_id - Unique form instance.
{% enddoc %}
{% render 'inchy-prelaunch-form', instance_id: instance_id, placement: 'product-guide' %}
