{%- comment -%}Keep the reference wordmark for standalone logos; inline brand names use the surrounding typography.{%- endcomment -%}
{%- if inline -%}
  INCHY
{%- else -%}
  <span class="inchy-logo" role="img" aria-label="inchy">{{ 'inchy-reference-wordmark.svg' | inline_asset_content }}</span>
{%- endif -%}
