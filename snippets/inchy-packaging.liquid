{% comment %}Shared FORM1 product packshot.{% endcomment %}
<img
  class="{{ image_class }}"
  src="{{ 'inchy-form1-reference.jpg' | asset_url }}"
  width="1126"
  height="1397"
  loading="{{ loading | default: 'lazy' }}"
  {% if loading == 'eager' %}fetchpriority="high"{% endif %}
  alt="INCHY FORM1 sacha inchi pouch — white resealable packaging with black inchy wordmark"
>
